{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "popover",
  "description": "Menu bar popover window: read-only view, emits navigation requests, hides itself on Escape. Window show/focus and positioning are handled natively in Rust.",
  "windows": [
    "popover"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-hide"
  ]
}
